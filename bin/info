#!/bin/bash

GREEN="\033[0;32m"
NO_COLOR="\033[0m"

info() {
    echo -e -n "$GREEN$1$NO_COLOR\t$2\n"
}

packages=$(pacman -Q | wc -l)
govener=$(cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_governor)

info "Packages:"    $packages
info "CPU Govener:" $govener
