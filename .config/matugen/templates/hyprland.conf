## Auto-generated via Matugen. ##

$primary    = rgb({{base16.base07.default.hex_stripped}})
$outline    = rgb({{base16.base04.default.hex_stripped}})
$error      = rgb({{base16.base06.default.hex_stripped}})
$background = rgb({{base16.base00.default.hex_stripped}})

general {
  col.active_border   = $primary
  col.inactive_border = $outline
}

group {
  col.border_active          = $primary
  col.border_inactive        = $outline
  col.border_locked_active   = $error
  col.border_locked_inactive = $outline

  groupbar {
    col.active          = $primary
    col.inactive        = $outline
    col.locked_active   = $error
    col.locked_inactive = $outline
    text_color          = $primary
    text_color_inactive = $outline
    }
}
